import  java.util.Random;


class Obraz_v1 {
    
    private int[] hist_parallel;    
    private int size_n;
    private int size_m;
    private char[][] tab;
    private char[] tab_symb;
    private int[] histogram;
    
    public Obraz_v1(int n, int m) {
	
	this.size_n = n;
	this.size_m = m;
	tab = new char[n][m];
	tab_symb = new char[94];
	hist_parallel = new int[94];
	final Random random = new Random();
	
	// for general case where symbols could be not just integers
	for(int k=0;k<94;k++) {
	    tab_symb[k] = (char)(k+33); // substitute symbols
	}

	for(int i=0;i<n;i++) {
	    for(int j=0;j<m;j++) {	
		tab[i][j] = tab_symb[random.nextInt(94)];  // ascii 33-127
		//tab[i][j] = (char)(random.nextInt(94)+33);  // ascii 33-127
		System.out.print(tab[i][j]+" ");
	    }
	    System.out.print("\n");
	}
	System.out.print("\n\n"); 
	
	histogram = new int[94];
   	clear_histogram();
	clear_histogram_parallel();
    }
    
    public void clear_histogram(){

	for(int i=0;i<94;i++) histogram[i]=0;

    }
    public void clear_histogram_parallel() {
    for(int i=0; i<94; i++) hist_parallel[i] = 0;
    }

///////////////////////////////////////////////////////////////////////////////////////////////////////////zliczanie znaków

    //jeden watek pokolei sprawdza ilosc wystapien każdego znaku po koleji
    public void calculate_histogram(){

	  for(int i=0;i<size_n;i++) {
	    for(int j=0;j<size_m;j++) {
		  for(int k=0;k<94;k++) {
		    if(tab[i][j] == tab_symb[k]) histogram[k]++;
		    //if(tab[i][j] == (char)(k+33)) histogram[k]++;	    
		  }

	    }
	  }

    }
    // każdy wątek sprawdza ilość wystapien jednego znaku i 94 wątki robią to jednocześnie
  public void calculate_histogram_parallel(char znak) {
    for(int i=0; i<size_n; i++) {
        for(int j=0; j<size_m; j++) {
            if(tab[i][j] == znak) {
                // Znajdź indeks znaku
                for(int k=0; k<94; k++) {
                    if(tab_symb[k] == znak) {
                        hist_parallel[k]++;
                        break;
                    }
                }
            }
        }
    }
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////porownanie

    public void compare_histograms() {
    boolean same = true;
    for(int i=0; i<94; i++) {
        if(histogram[i] != hist_parallel[i]) {
            same = false;
            
        }
    }
    	if(same) {
        System.out.println("\n Histogramy sa identyczne - obliczenia poprawne!");
        }
	else {
        System.out.println("\n Histogramy sie roznia!");
    	}
    }

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////wypisywanie
    public synchronized void print_histogram_parallel(char znak, int thread_id) {
    int index = -1;
    for(int k=0; k<94; k++) {
        if(tab_symb[k] == znak) {
            index = k;
            break;
        }
    }
    
    if(index != -1) {
        System.out.print("Wątek " + thread_id + ": " + znak + " "+ hist_paralell[index] + " ");
        for(int i=0; i<hist_parallel[index]; i++) {
            System.out.print("=");
        }
        System.out.println();
    }
}

    public void print_histogram(){
	
	for(int i=0;i<94;i++) {
	    System.out.print(tab_symb[i]+" "+histogram[i]+"\n");	    
	    //System.out.print((char)(i+33)+" "+histogram[i]+"\n");	    
	}

    }

}
